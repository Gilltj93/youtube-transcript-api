services:
  - type: web
    name: youtube-transcript-api
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn app:app
    region: oregon
    plan: free
    branch: master
